import heightmap from './heightmap';

import { types as heightmapTypes } from './heightmap';

export const actionTypes = {
  ...heightmapTypes,
};

const actions = {
  ...heightmap,
};

export default actions;
